apiVersion: v1
kind: Service
metadata:
  name: csharp-web-service
spec:
  selector:
    app: csharp-web
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30080
  type: NodePort