apiVersion: apps/v1
kind: Deployment
metadata:
  name: csharp-web
  labels:
    app: csharp-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: csharp-web
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: csharp-web
    spec:
      containers:
      - name: csharp-web
        image: docker.io/library/csharp-web:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        - name: ConnectionStrings__DefaultConnection
          value: "Host=postgres;Port=5432;Database=brasil_burger;Username=postgres;Password=postgres"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"